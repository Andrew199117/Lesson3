const age: number = 23;
const days: number = age * 365;
const hours: number = days;
const minutes: number = hours * 60;
const seconds: number = minutes * 60;

//добавить минуты

console.log(`Мне ${days} дней ${hours} часов ${minutes} минут и ${seconds} секунд`);
